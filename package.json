{
  "private": true,
  "name": "declarative-hue",
  "version": "0.0.2",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Steve Barker",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
